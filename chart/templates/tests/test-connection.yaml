apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itl.website.fullname" . }}-test-connection"
  labels:
    {{- include "itl.website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itl.website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
