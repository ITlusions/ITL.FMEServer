apiVersion: v1
kind: Service
metadata:
  name: {{ include "itl.website.fullname" . }}
  labels:
    {{- include "itl.website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itl.website.selectorLabels" . | nindent 4 }}
