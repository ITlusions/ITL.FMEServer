<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.itlusions.atak.calamiteiten">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/ATAKPluginTheme">

        <!-- ATAK Plugin Declaration -->
        <service
            android:name=".CalamiteitenMapComponent"
            android:enabled="true"
            android:exported="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.atakmap.app.ATAK_PLUGIN" />
            </intent-filter>
            <meta-data
                android:name="plugin-api"
                android:value="4" />
            <meta-data
                android:name="plugin-version"
                android:value="1.0.0" />
            <meta-data
                android:name="plugin-description"
                android:value="Nederlandse calamiteitenbeheer en rampenbestrijding voor ATAK" />
            <meta-data
                android:name="plugin-icon"
                android:resource="@drawable/ic_plugin" />
        </service>

        <!-- Sync Service -->
        <service
            android:name=".sync.SyncService"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot receiver for auto-start sync -->
        <receiver
            android:name=".sync.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/settings_title"
            android:theme="@style/ATAKPluginTheme"
            android:exported="false" />

    </application>

</manifest>
